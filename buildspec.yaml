version: 0.2

phases:
  install:
    commands:
        - script: npm install
  # build:
  #   commands:
  #     - echo Build started on `date`
  #     - echo Compiling the Python code...
  #     - node index.js
  post_build:
    commands:
      - script: node index.js
artifacts:
  files:
    - index.js
    - package.json